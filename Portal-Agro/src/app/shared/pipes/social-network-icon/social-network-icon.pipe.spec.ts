import { SocialNetworkIconPipe } from './social-network-icon.pipe';

describe('SocialNetworkIconPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialNetworkIconPipe();
    expect(pipe).toBeTruthy();
  });
});
