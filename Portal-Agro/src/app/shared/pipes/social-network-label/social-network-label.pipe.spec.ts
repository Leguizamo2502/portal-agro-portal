import { SocialNetworkLabelPipe } from './social-network-label.pipe';

describe('SocialNetworkLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialNetworkLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
