import { StatusTranslatePipePipe } from './status-translate-pipe.pipe';

describe('StatusTranslatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTranslatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
