import { IfLoggedOutDirective } from './if-logged-out.directive';

describe('IfLoggedOutDirective', () => {
  it('should create an instance', () => {
    const directive = new IfLoggedOutDirective();
    expect(directive).toBeTruthy();
  });
});
